import VerticalBar from "../UIComponents/VerticalBar";

const ModelDecision = ({ decision, setDecision }) => {
  return (
    <div className="Homepage-container">
      <VerticalBar />

      <div className="Homepage-remaining-content"></div>
    </div>
  );
};

export default ModelDecision;
